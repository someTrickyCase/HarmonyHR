// import { Session } from "inspector/promises";
// import { useSession } from "./store";
// import React from "react";

// const StoreProvider = ({ session, children }: { session: Session; children: React.ReactNode }) => {
//   useSession((state) => state.updateSession(session));
//   return children;
// };

// export default StoreProvider;
